/*
Write a function, multiply, that takes any number of arguments 
and multiplies them all together. If there is only one number, 
it will return that number. If no numbers are passed in, it will return 0. For example:

multiply() // 0
multiply(1) // 1
multiply(1, 2) // 2
multiply(1, 2, 3) // 6
multiply(1, 2, 3, 4) // 24
*/

function multiply () {

}