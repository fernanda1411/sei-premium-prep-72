/*
We are going to complete a function called reduce. 
It should behave similarly to the native array method of the 
same name from the previous lesson. However, your implementation should 
not use Array.prototype.reduce, i.e. should not look like this:

function reduce(array, callbackFunction, startingValue) {
  return array.reduce(callbackFunction, startingValue);
}
Here is an example to get a sense of what reduce should do.

var input = [1, 2, 3, 4];
var output = reduce(input, function(sum, value) {
  return sum + value;
}, 0)

console.log(output); // 10

*/

function reduce(array, callbackFunction, startingValue) {
  // your code here
}
